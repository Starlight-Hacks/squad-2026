info:
  name: whatsapp_initiate_payment
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/webhooks/twilio/whatsapp"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
