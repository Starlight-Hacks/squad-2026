part of '../assets.dart';

class SvgStrings {
  static final SvgStrings instance = SvgStrings._();
  SvgStrings._();
  static const String _svgPrefix = "assets/svgs/";
  static String _withPrefix(String name) => "$_svgPrefix$name";

  // ============================================================================
  // SVGS
  // ============================================================================
}
